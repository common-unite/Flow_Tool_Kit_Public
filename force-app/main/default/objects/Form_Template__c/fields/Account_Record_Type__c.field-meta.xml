<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_Record_Type__c</fullName>
    <description>Record type applied to Accounts created during conversion. The convert Flow reads this to stamp RecordTypeId; '-- Use Default --' lets Salesforce assign the running user's default Account record type.</description>
    <inlineHelpText>Pick the record type for Accounts this form creates. Leave on '-- Use Default --' to use the standard default for the converting user.</inlineHelpText>
    <label>Account (Record Type)</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Picklist</type>
    <valueSet>
        <restricted>true</restricted>
        <valueSetDefinition>
            <sorted>false</sorted>
            <value>
                <fullName>-- Use Default --</fullName>
                <default>true</default>
                <label>-- Use Default --</label>
            </value>
        </valueSetDefinition>
    </valueSet>
</CustomField>
