<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>Maps a source object (e.g., Campaign) to Form Template override fields. Create one record per source object. Map fields for name, availability, theme, pre-fill template, confirmation message, offline message, and email template. Only the Source Object and Form Template Field are required — all other mappings are optional. Unmapped fields fall back to the Form Template defaults.</description>
    <label>Form Template Source</label>
    <pluralLabel>Form Template Sources</pluralLabel>
    <visibility>Public</visibility>
</CustomObject>
